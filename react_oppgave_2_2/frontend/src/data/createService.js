/*

import client from './client' 
export const createService = async () => {
  const { title, slug, slogan, ingress, box} = createNewService

  try {
    await client.create ({_type: 'service', title, slug, slogan, ingress, box})
  } catch (error) {
    throw new Error(error)
  }
}


Have slug be generated
*/
